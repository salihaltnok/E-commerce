@if (Context.Session.GetString("KullaniciAdi") != null)
{
    <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            @Context.Session.GetString("KullaniciAdi")
        </a>
        <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
            @if (Context.Session.GetString("IsAdmin") == "True")
            {
                <li><a class="dropdown-item" href="@Url.Action("Index", "Admin")">Admin Panel</a></li>
                <li><hr class="dropdown-divider"></li>
            }
            <li><a class="dropdown-item" href="@Url.Action("Profil", "Kullanici")">Profilim</a></li>
            <li><hr class="dropdown-divider"></li>
            <li><a class="dropdown-item" href="@Url.Action("CikisYap", "Kullanici")">Çıkış Yap</a></li>
        </ul>
    </li>
}
else
{
    <li class="nav-item">
        <a class="nav-link" href="@Url.Action("GirisYap", "Kullanici")">Giriş Yap</a>
    </li>
    <li class="nav-item">
        <a class="nav-link" href="@Url.Action("KayitOl", "Kullanici")">Kayıt Ol</a>
    </li>
}